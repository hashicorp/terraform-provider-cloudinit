dist: trusty
sudo: required
services:
  - docker
language: go
go:
- stable
- tip

env:
  - GOFLAGS=-mod=vendor TF_ACC_TERRAFORM_VERSION=0.14.0-beta2

script:
- make test
- make vet
- make testacc

branches:
  only:
  - master
